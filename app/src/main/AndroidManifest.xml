<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".Playlists" />
        <activity
            android:name=".Chill"
            android:label="Chill" />
        <activity
            android:name=".Retro"
            android:label="Retro" />
        <activity
            android:name=".Oldies"
            android:label="Oldies" />
        <activity
            android:name=".Playlists1Activity"
            android:label="@string/chill_beats" />
        <activity
            android:name=".Playlists2Acticity"
            android:label="@string/heartbeats" />
        <activity
            android:name=".Playlists3Activity"
            android:label="@string/play_the_hits" />

        <activity
            android:name=".NowPlayingActivity"
            android:label="Playing" />

    </application>

</manifest>